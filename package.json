{
  "name": "codepunk",
  "version": "1.0.0",
  "description": "cli for codepunk package",
  "bin": "dist/index.js",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.evn.NODE_ENV=\\\"production\\\""
  },
  "author": "lullabyX",
  "license": "ISC",
  "dependencies": {
    "@codepunk/client": "^1.0.0"
  },
  "devDependencies": {
    "@codepunk/local-api": "^1.0.0",
    "@types/node": "^18.11.0",
    "commander": "^9.4.1",
    "esbuild": "^0.15.11"
  }
}
